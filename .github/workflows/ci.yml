name: quality

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Reconstruct and build v0.3
        run: bash cloudflare-build.sh
      - uses: actions/upload-artifact@v4
        with:
          name: project-sovereign-v0.3-dist
          path: dist
          if-no-files-found: error
      - name: Export reconstructed source for v0.4 development
        run: tar -czf project-sovereign-v0.3-source.tar.gz -C .cf-build project-sovereign
      - uses: actions/upload-artifact@v4
        with:
          name: project-sovereign-v0.3-source
          path: project-sovereign-v0.3-source.tar.gz
          if-no-files-found: error
